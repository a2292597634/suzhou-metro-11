services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: metro123
      POSTGRES_DB: suzhou_metro
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "postgresql://postgres:metro123@postgres:5432/suzhou_metro?schema=public"
      PORT: "3000"
      AUTH_TOKEN: ${AUTH_TOKEN:?请设置 AUTH_TOKEN 环境变量}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:3000}
      SESSION_SECRET: ${SESSION_SECRET:?请设置 SESSION_SECRET 环境变量}
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
