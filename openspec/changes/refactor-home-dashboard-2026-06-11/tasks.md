## 1. 首页数据与渲染纯函数

- [x] 1.1 在 `js/modules/home.js` 中定义可测试的趋势点位、Y 轴刻度、站名换行和经营概览过滤纯函数
- [x] 1.2 保持 `calcHomeStats()` 的现有统计口径，并补齐首页空数据所需的安全默认值
- [x] 1.3 重构首页 HTML 生成逻辑，确保所有动态文本通过既有转义工具或安全 DOM API 输出
- [x] 1.4 固定业务口径：商业点位排除多经点位、已出租仅指营业中、装修单列、出租率包含装修

## 1.T 首页数据与渲染纯函数测试

- [x] 1.T.1 在 `tests/home.test.js` 先编写趋势点位、动态刻度、站名换行和过滤条件的失败测试（Red）
- [x] 1.T.2 编写多经点位排除、面积汇总、装修计入出租率和空数据的失败测试（Red）
- [x] 1.T.2a 编写站点名、商铺名、租户名和属性字段的 XSS 失败测试（Red）
- [x] 1.T.3 运行 `npx vitest run tests/home.test.js` 并确认新增测试失败（Red 验证）
- [x] 1.T.4 实现 1.1-1.3 的最小代码并运行测试通过（Green）
- [x] 1.T.5 在测试保护下消除重复计算和命名歧义（Refactor）

## 2. 共享品牌导航

- [x] 2.1 更新 `js/modules/nav.js`，使用压缩版 `assets/design/logo-concept-02-v1-160.png` 和确认的平台品牌文案
- [x] 2.2 将三个现有路由的导航文案更新为“经营总览”“商业分析”“线路资产”，保持 href 不变
- [x] 2.3 更新 `css/platform.css` 的桌面品牌区布局，并保留 768px 及以下现有底部导航行为
- [x] 2.4 保留刷新按钮、`#datasource-indicator` 和 `datasource:change` 事件行为
- [x] 2.5 为 Logo 声明固有宽高，并移除三个正式页面和 `platform.css` 中不符合 CSP 的外部字体/样式依赖

## 2.T 共享品牌导航测试

- [x] 2.T.1 在 `tests/nav.test.js` 先编写 Logo 路径、平台文案、导航文案和 href 的失败测试（Red）
- [x] 2.T.2 编写激活态、数据来源指示器和移动端导航结构的失败测试（Red）
- [x] 2.T.2a 编写 Logo 固有尺寸和正式页面无外部字体链接的失败测试（Red）
- [x] 2.T.3 运行 `npx vitest run tests/nav.test.js` 并确认新增测试失败（Red 验证）
- [x] 2.T.4 实现 2.1-2.4 的最小代码并运行测试通过（Green）
- [x] 2.T.5 在测试保护下整理导航 DOM 创建函数和响应式类名（Refactor）

## 3. 首页结构与 KPI 看板

- [x] 3.1 重构 `index.html`，只保留首页综合看板挂载结构并加载 `css/home-dashboard.css`
- [x] 3.2 新增 `css/home-dashboard.css`，实现确认稿的头图、卡片、字体层级、颜色、边框、圆角和阴影
- [x] 3.3 在 `home.js` 渲染经营总览头图与 5 个 KPI，全部读取 `calcHomeStats()` 结果
- [x] 3.4 使用压缩版 `assets/design/commercial-overview-bg-v1.jpg` 作为低对比度头图背景
- [x] 3.5 删除首页旧的独立排名区和旧线路圆点速览挂载结构
- [x] 3.6 删除原型中无现有功能支撑的“通知公告”“点位台账”入口；如保留行动按钮，仅链接现有页面
- [x] 3.7 压缩首页背景和 Logo 资源，优先将正式加载的图片总量控制在 700 KB 内

## 3.T 首页结构与 KPI 测试

- [x] 3.T.1 在 `tests/integration/home-dashboard.test.js` 先编写首页区块顺序和 KPI 真实数据映射的失败测试（Red）
- [x] 3.T.2 编写空站点数组时 KPI 为 0 且显示空状态的失败测试（Red）
- [x] 3.T.2a 编写首页不存在无效入口且所有行动链接均可访问的失败测试（Red）
- [x] 3.T.3 运行 `npx vitest run tests/integration/home-dashboard.test.js` 并确认新增测试失败（Red 验证）
- [x] 3.T.4 实现 3.1-3.5 的最小代码并运行测试通过（Green）
- [x] 3.T.5 在测试保护下整理首页区块渲染函数（Refactor）

## 4. 站点商业趋势

- [x] 4.1 使用原生 SVG 渲染全部站点的商业点位、已出租、空置三条直线折线
- [x] 4.2 根据最大商业点位动态生成 Y 轴刻度与低对比度虚线网格
- [x] 4.3 实现站名每 5 个字符换行、画布首尾留白和默认隐藏数据圆点
- [x] 4.4 实现站点悬停参考线、三个空心圆点和详情卡，并在离开后保持最后状态
- [x] 4.5 实现仅左右各 20% 边缘触发的平滑画布移动，中央 60% 保持静止
- [x] 4.6 在 `prefers-reduced-motion` 下禁用非必要折线和移动动画
- [x] 4.7 为触屏提供原生横向滚动和点击详情，为键盘提供站点焦点与详情交互
- [x] 4.8 在趋势重新初始化时清理旧事件监听和 `requestAnimationFrame`

## 4.T 站点商业趋势测试

- [x] 4.T.1 在 `tests/integration/home-dashboard.test.js` 先编写全部站点、三条路径和动态刻度的失败测试（Red）
- [x] 4.T.2 编写悬停详情内容、圆点显示和详情保持的失败测试（Red）
- [x] 4.T.3 编写中央区域不移动、边缘区域移动且不越界的失败测试（Red）
- [x] 4.T.3a 编写触屏点击、键盘聚焦、减少动画和重复初始化清理的失败测试（Red）
- [x] 4.T.4 运行趋势相关测试并确认失败（Red 验证）
- [x] 4.T.5 实现 4.1-4.6 的最小代码并运行测试通过（Green）
- [x] 4.T.6 在测试保护下提取动画状态和坐标计算函数（Refactor）

## 5. 站点经营概览

- [x] 5.1 渲染全部站点的价值等级、商铺、多经点位、总面积、已租面积、已租/装修/空置和出租率
- [x] 5.2 实现固定高度内部滚动、固定表头和站点线路序号
- [x] 5.3 实现“全部站点”“S / A 级”“低出租率”筛选和站名搜索
- [x] 5.4 保留站点主行展开商铺明细，显示编号、名称、属性、面积、租户和状态
- [x] 5.5 确保搜索筛选同时控制主行和详情行，并在切换站点时仅保留一个详情展开
- [x] 5.6 确认表格不包含“数据状态”“最近更新”等不存在字段
- [x] 5.7 实现搜索词去除前后空格、搜索与筛选组合、无结果空态
- [x] 5.8 为展开按钮同步 `aria-expanded` 并支持 Enter/Space 操作

## 5.T 站点经营概览测试

- [x] 5.T.1 在 `tests/integration/home-dashboard.test.js` 先编写真实列名、全部站点和面积数据的失败测试（Red）
- [x] 5.T.2 编写站名搜索、S/A 筛选和出租率小于 80% 筛选的失败测试（Red）
- [x] 5.T.3 编写主行与详情行展开、切换和过滤联动的失败测试（Red）
- [x] 5.T.3a 编写组合筛选、空结果、键盘展开和 ARIA 状态的失败测试（Red）
- [x] 5.T.4 运行经营概览相关测试并确认失败（Red 验证）
- [x] 5.T.5 实现 5.1-5.6 的最小代码并运行测试通过（Green）
- [x] 5.T.6 在测试保护下整理表格模板和事件委托（Refactor）

## 6. 响应式与浏览器流程

- [x] 6.1 完成 1440px、980px、640px 和 390px 的首页响应式规则，并保持共享导航现有 768px 移动端断点
- [x] 6.2 确保趋势和经营表仅在自身容器内滚动，`body` 不产生水平溢出
- [x] 6.3 验证共享导航在 `index.html`、`data-viz.html` 和 `battle-map.html` 均正确显示
- [x] 6.4 验证首页资源符合现有 CSP `'self'` 限制且无控制台错误

## 6.T 响应式与 E2E 测试

- [x] 6.T.1 更新 `tests/e2e/home-flow.test.js`，先编写品牌区、看板区块和全部站点显示的失败测试（Red）
- [x] 6.T.2 编写趋势悬停、边缘移动、搜索筛选和展开详情的失败 E2E 测试（Red）
- [x] 6.T.3 编写 1440px、980px、640px 与 390px 视口无页面级水平溢出的失败测试（Red）
- [x] 6.T.3a 监听控制台和失败请求，断言无 CSP 阻止、外部字体请求或运行时错误（Red）
- [x] 6.T.4 运行 `npx vitest run tests/e2e/home-flow.test.js` 并确认新增测试失败（Red 验证）
- [x] 6.T.5 实现 6.1-6.4 的最小代码并运行 E2E 测试通过（Green）
- [x] 6.T.6 在浏览器中对照 `design/prototype-v1.html` 调整视觉细节且保持测试通过（Refactor）

## 7. 完整验证与文档一致性

- [x] 7.1 运行 `npx vitest run` 并修复全部回归失败
- [x] 7.2 运行 `node scripts/check-test-coverage.js` 并确认模块覆盖检查通过
- [x] 7.3 在桌面浏览器和局域网手机浏览器手动验证首页核心流程
- [x] 7.4 检查 `proposal.md`、`design.md`、两份 `spec.md` 和 `tasks.md` 与实际实现一致
- [x] 7.5 展示最终变更文件列表、测试结果和建议 commit message，等待用户明确确认后再 commit

# 测试检查清单

> 本清单嵌入每个 change 的 tasks.md 末尾，作为测试验收的硬性门槛。所有复选框必须打勾，change 才算完成。

---

## 检查项

### 阶段一：Propose（规划阶段）

- [x] **测试策略已定义**：在 design.md 或 proposal.md 中说明了本 change 需要哪一层测试（单元/集成/E2E）
- [x] **测试任务已拆分**：tasks.md 中每个功能模块任务组都有对应的测试任务（如「2.1 实现 xxx」对应「2.T 测试 xxx」）
- [x] **测试基础设施已确认**：如果本 change 需要新的测试库/配置，已在 tasks 中列出

### 阶段二：Apply（实施阶段）

- [x] **TDD 顺序已遵守**：每个功能点执行了 Red → Green → Refactor
- [x] **单元测试已编写**：每个新增/修改的模块都有对应的 `tests/<模块名>.test.js`
- [x] **集成测试已编写**（如涉及多模块联动）：有 `tests/integration/<场景>.test.js`
- [x] **E2E 测试已编写**（如涉及页面/核心流程）：有 `tests/e2e/<场景>.test.js`
- [x] **测试命名规范**：测试文件命名符合 `tests/<模块名>.test.js` 或 `tests/<层级>/<场景>.test.js`
- [x] **测试描述清晰**：每个 `it()` 描述读起来像一句完整的中文断言（如「应该从无文件名路径解析首页」）

### 阶段三：验证阶段

- [x] **全部测试通过**：运行 `npm test` 输出 `Tests N passed (N)`，零失败
- [x] **覆盖率检查通过**：运行 `node scripts/check-test-coverage.js`，输出「所有模块均已覆盖」
- [x] **无测试作弊**：没有为了通过而修改测试期望值、没有跳过关键断言、没有 mock 掉核心业务逻辑
- [x] **手动验证完成**：在真实浏览器中验证了核心场景（至少一次）

### 阶段四：归档阶段

- [x] **测试文件已提交**：所有测试文件已纳入 git 版本控制
- [x] **CI 脚本已更新**（如有）：package.json 的 test 脚本能正确运行新增测试
- [x] **文档已更新**：如测试策略或基础设施有变化，已同步更新 `openspec/testing-strategy.md`

---

## 豁免规则

以下情况可以豁免部分或全部测试，但必须在 change 中明确标注并说明理由：

| 豁免场景 | 可豁免的测试 | 必须做的 | 标注位置 |
|---------|------------|---------|---------|
| 纯 CSS 样式调整（颜色、间距、圆角） | 全部 | 手动视觉验证 | tasks.md 顶部 |
| 纯文案/标点修改 | 全部 | 手动阅读确认 | tasks.md 顶部 |
| 配置文件修改（不涉及逻辑） | 全部 | 配置生效验证 | tasks.md 顶部 |
| 已有模块的微小重构（无行为变更） | E2E | 单元测试需全部通过 | tasks.md 对应任务旁 |

> **注意**：豁免不等于跳过验证。即使豁免测试，也必须通过其他方式确认变更正确。
